@model WhoIsRunningThisWeek.UpcomingSessionsViewModel


@{
    ViewBag.Title = "Edit";
}
<h2>@ViewBag.Title.</h2>
<h3>@ViewBag.Message</h3>

@using (Html.BeginForm(actionName: "edit", controllerName: "Home", method: FormMethod.Post, htmlAttributes: new { encType = "multipart/form-data" }))
{
    <div style="padding: 15px">
        This Week

        @Html.EnumDropDownListFor(model => model.ThisWeekSession.Person)

        @*<select name="ddlThisWeek" id="ddlThisWeek">

                @foreach (var person in PeopleManagement.People)
                {
                    var selected = person.Name == Model.ThisWeekSession.Person.Name;

                    <option value="@person.Name" id="@person.Name" selected="@selected">
                        @person.Name
                    </option>
                }


            </select>*@
    </div>

    <div style="padding: 15px">
        Next Week

        @Html.EnumDropDownListFor(model => model.NextWeekSession.Person)

        @*<select name="ddlNextWeek" id="ddlThisWeek">

                @foreach (var person in PeopleManagement.People)
                {

                    var selected = person.Name == Model.NextWeekSession.Person.Name;

                    <option value="@person.Name" id="@person.Name" selected="@selected">
                        @person.Name
                    </option>
                }
            </select>*@
    </div>

    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <input type="submit" value="Save" class="btn btn-default" />
        </div>
    </div>
}

